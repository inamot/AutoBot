 bibtex manual  
   
   pops top two  string  literals changes case second 
   according specifications first follows   note word 
   `letters next sentence refers only those brace-level 0 top-most brace level characters changed  except perhaps special characters   described section 4 first literal string `t converts lower case letters except very first 
   character string leaves alone except first 
   character following any colon nonnull white space leaves alone s string `l converts letters lower case s string `u converts letters upper case pushes resulting string either type incorrect complains pushes null string both types correct specification string e first string  isn't one legal ones merely pushes second back onto stack complaining   another 
   note ignores case differences specification string example strings t t equivalent purposes built function   
   
   christopher think another grammar parser horrible  
   
    net sf jabref logic bst changecasefunction bstfunction changecasefunction vm vm vm   vm  
      
 
     override execute bstentry context    
        stack object  stack   vm getstack stack size     2 vmexception  not enough operands on stack operation change object o1   stack pop o1 string  o1  length      1 vmexception  a format of length 1 is needed change object o2   stack pop o2 vmexception  a is needed as second parameter change format o1  tolowercase   charat 0 s o2  
 
        stack push bibtexcasechanger changecase s  format_mode getformatmodeforbstformat format     
      
 
 