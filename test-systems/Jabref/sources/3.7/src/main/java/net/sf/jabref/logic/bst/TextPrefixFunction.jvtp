 built_in  function  \  text prefix\$   pops top two literals integer literal  pop_lit1 string literal order pushes substring most   pop_lit1  consecutive text 
 characters starting beginning string function similar substring one considers accented 
 character precisely special character''$\ $  even s missing matching  right_brace single text character many  ascii_code  characters actually 
 comprises function doesn't consider braces text 
 characters  furthermore function appends any needed matching 
  right_brace s any types incorrect complains pushes null string  
   
    net sf jabref logic bst textprefixfunction bstfunction textprefixfunction vm vm vm   vm  
      
 
     override execute bstentry context    
        stack object  stack   vm getstack stack size     2 vmexception  not enough operands on stack operation text prefix$    
          
 
        object o1   stack pop o1 integer     
            vm warn  an integer is needed as first parameter to text prefix$    
            stack push object o2   stack pop o2 vm warn  a is needed as second parameter to text prefix$    
            stack push stack push bibtextextprefix textprefix  integer  o1 o2  vm    
      
 