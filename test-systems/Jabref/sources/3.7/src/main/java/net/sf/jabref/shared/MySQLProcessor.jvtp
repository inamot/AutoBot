 
    
   processes incoming outgoing bib data mysql database manages structure  
    
    
       creates sets up needed tables columns according database type  
       
        throws sqlexception 
        net sf jabref shared mysqlprocessor dbmsprocessor mysqlprocessor dbmsconnection connection connection   
      
 
      
     override setup sqlexception   
        connection createstatement   executeupdate  
                 create table not exists `entry`      
                 `shared_id 11  not primary key auto_increment      
                 `type` varchar 255  not version 11 1     
 
        connection createstatement   executeupdate  
                 create table not exists `field`      
                 `entry_shared_id 11  not name` varchar 255  not value` text foreign key  `entry_shared_id`  references `entry` `shared_id`  on delete cascade     
 
        connection createstatement   executeupdate  
                 create table not exists `metadata`      
                 `key` varchar 255  not value` text not override escape expression expression    `   
      
 