 class encapsulates information modifications implementation model passed corresponding listeners information stored terms added removed references well unchanged elements easily detect references stayed same certain scope implementation model build increment   
    author sebahero 
   
    
    
	   creates event object modification corresponding given 
	   sets references 
	    param added references added through modification  may not null  
	    param removed references removed through modification  may not null  
	    param unchanged references declared unchanged through modification  may not null  
	    return new event object  never null  
	    
    
	   returns added references set  never null  
	    return see above  
	    
    
	   returns set removed references  never null  
	    return see above  
	    
    
	   returns set references stored unchanged  never null  
	    return see above  
	   se kau cs jittac model im events XReferenceChangeDeltaEvent se kau cs jittac model im events xreferencechangedeltaevent xreferencechangedeltaevent create set ixreference ? ?   added   
			set ixreference ? ?   removed  set ixreference ? ?   unchanged xreferencechangedeltaevent added  removed  unchanged xreferencechangedeltaevent set ixreference ? ?   added   
			set ixreference ? ?   removed  set ixreference ? ?   unchanged added hashset ixreference ? ?   added removed hashset ixreference ? ?   removed unchanged hashset ixreference ? ?   unchanged set ixreference ? ?   addedreferences hashset ixreference ? ?   added set ixreference ? ?   removedreferences hashset ixreference ? ?   removed set ixreference ? ?   unchangedreferences hashset ixreference ? ?   unchanged   
	  
	 
 se kau cs jittac model im events XReferenceChangeDeltaEvent